import styled from 'styled-components'

const Paginator = styled.div`
    display: inline-block;
`

const Page = styled.a`
    color: black;
    float: left;
    cursor: pointer;
    padding: 8px 16px;
    text-decoration: none;
`

export { Paginator, Page }